# 项目进展与待办（截至 2026-09-12 收工）

> 这份是**工作记录/交接单**，用于下次接着干。与 `docs/` 下其它设计稿不同，这份我破例提交进了 git
> —— 因为它是"进度档案"而不是设计草稿，需要跨设备可见。不想要的话跟我说，我撤掉。

---

## 〇、协作约定（最高优先，开工先读）

> 规则本体已写进项目级 `AGENTS.md`（开工自动读），这里再留一份，免得被忽略。

**用户 2026-09-12 明确要求：「我的建议或者规划不合理的时候你要及时找我确认，这个很重要」。**

遇到「按用户说法做会出问题」的情况，**先停下来确认，不要硬着头皮做完再返工**。提的时候要给：

1. **证据**：代码/测试/线上数据的实际结果（文件行号、断言输出、接口返回），不是感觉；
2. **代价分类**：做不出来 / 做得出来但体验坏 / 做得出来但性价比低，分开讲；
3. **替代方案**：至少一个可执行的备选（缩范围、分两期、换实现）及其差异；
4. **方向性决策先问**：改玩法规则、改数值口径、改奖励体系、删已有功能、大范围改 UI —— 一律等确认；
5. **例外**：明显 bug（关卡不可能通关、接口 500、测试红、文案错字）不用等确认，修完连回归护栏一起汇报。

---

## 一、今日已完成（全部已推送）

| 提交 | 内容 |
|---|---|
| `c2658f6` | **R1 星级档位死区修复**：3 命 + 90/70/40 导致 1 星永远拿不到 → 改 5 命 + 90/70/60；新增星级可达性回归护栏 |
| `184cb1d` | **修复接口冒烟的假绿灯**：原先只看 HTTP 200 不校验业务码，导致线上成绩链路挂了也报 12/12 全绿 |
| `4bdf37f` | **修复 `/api/ranklist/me` 线上 5000**：Sequelize v6 已移除 `$gt/$ne` 别名，会生成 `= '[object Object]'` 的 SQL |
| `a390cd3` | **R2 错题接入主玩法抽题**：新增 `utils/review.js`，主玩法以 25% 概率优先出待复习错题（艾宾浩斯闭环） |
| `720259d` | 关卡/形态一期：形态内联为模式栏、首页「继续挑战」直达对局 |
| `da03e7d` | **R3/R4/R5**：答错反馈延长到 1.2s + 选项标绿标红；结算页本局错题回顾；对局暂停按钮 |
| `1af3367` | **关卡页一期重构**：蛇形地图 → 竖排关卡列表；删除三种闯关形态（换皮）；修复「已解锁却显示锁定」 |
| `995d68c` | **固定关卡扩题库**：数独 20→60 关、算 24 点 10→60 关；**修复 2048 三关数学上不可能通关** |
| `c8257fc` | **段位扩展为 8 大段 × 9 小级 = 72 级**（按星星晋升，满级 527 星）；移除我的页重复入口 |
| `707d0c1` | 玩法落地 1/6：**口算冲刺**（60 秒限时 · 连击倍率） |
| `b376471` | 玩法落地 2/6：**记忆矩阵**（亮格记忆，程序生成关卡） |
| `cf061ac` | 玩法落地 3~6：**字母拼词 / 成语拼字 / 算式天平 / 一笔画**（子代理产出，含 3 个单测共 3959 条断言） |
| `edf7913` | 2048 步数预算放宽（上限仍 512）；首页「累计星星」改为云端口径，与我的页统一 |
| `c443a9f` | **补 8 枚段位徽章图**（`e2e/gen_rank_badges.py` 程序化生成）+ 我的页展示 |

### 本轮修掉的三个"真 bug"（都不是需求，是走查/测试时挖出来的）
1. **2048 有三关不可能通过**：步数预算线性 +6 增长，而难度是翻倍增长 —— 1024/54、2048/64 数学上无解。
2. **成绩链路线上全挂**：生产库缺 `scores.game_type/type_key` 两列（用户手工执行 SQL 已修），
   另有 `/api/ranklist/me` 因 Sequelize v6 旧式操作符而 5000。
3. **口算冲刺会出负数题**：减法档 `8 − 9 = −1`，单测当场抓出。

---

## 一·补 收工后继续推进的进展（2026-09-12 晚场）

| 提交 | 内容 |
|---|---|
| `fcd98b4` | **算 24 点改「合并式」**：选中两张 → 点运算符即合并成一张结果牌并自动选中，最后剩一张 = 24 过关（撤销/重来/提示一步；不设命数） |
| `2cf2abc` | **贪吃蛇手感修复**：300ms→450ms 降速、加 900ms 开局缓冲、`disableScroll` 禁页面滚动、**撞墙改为穿到对面** |
| `1e74cfe` | **贪吃蛇点方向操控**：点蛇头上/下/左/右哪一侧就往哪边走（走 `onCellTap`，180° 反向被拒） |
| `08d1cef` | **贪吃蛇去掉滑动转向**：有了点方向之后滑动只剩误触，删除 `onTouchStart/onTouchEnd`；E2E 33→50 条断言（新增点方向 6 条 + 穿墙回归 5 条） |
| （本轮） | **贪吃蛇改为相对转向**：以蛇头朝向为轴线，点左/右侧拐 90°、点正前方直行、点正后不掉头；E2E 50→52 条 |
| （本轮） | **两份待确认文档**：`docs/美术素材需求与豆包提示词.md`（72 枚徽章 / 24 套皮肤 / 32 张成就图的规格＋豆包提示词）、`docs/挑战关卡规划-待确认.md`（把所有题库类玩法纳入挑战主线，按年级生成关卡） |
| （本轮） | **挑战主线 P1 落地**（用户「按你建议的来」）：新增 `utils/rng.js` + `utils/challenge.js`；首页「继续挑战」升级为「玩法 · 学段 · 第 N/30 关」并按玩法跳转；关卡页默认视图 = 30 关主线（带玩法标签），原题型分类保留为自由练；字母射击/字母拼词/词语连连看接入挑战模式（固定种子题面 + 挑战星级存档 + 段位同步）；老存档一次性迁移。顺带修掉字母拼词「1 星档不可达 + 失败也发星」的老缺陷 |
| （本轮） | **需求② 错题本优化**：`GET /api/wrong/list` 支持 scope/page/pageSize（**不传参数保持原形状**，非法参数 4000）；新增 `POST /api/wrong/remove`（校验归属 + **幂等**）；错题本列表改服务端分页 + 每条「移除」；复习答对后弹「移出错题本 / 先保留」。新增 `server/wrong-book.js` 与 `utils/wrong-book-view.js` 两个纯逻辑模块 + 26 条单测用例（160 断言）；冒烟脚本新增错题本完整链路（对真实部署跑） |
| （本轮） | **需求④ 成就扩充 6 → 37 条**：新增 `server/achievements.js` 作为唯一口径（定义+判定+进度全在代码里，**零表结构变更**，绕开原 `conditionType` ENUM 的 DDL 负担）；7 分类（答题/关卡/玩法/习惯/错题/段位/自定义）；`/api/achievement/list` 返回数组 + category/progress/unlockedAt（兼容老客户端），check 幂等；成就页加分类 chips、进度条、图标缺图回退 emoji。新增 `utils/achievement-view.js` + 28 条单测用例（454 断言），冒烟新增成就契约链路 |
| （本轮） | **素材到货 + 包体治理**：成就图标 38 张 / 段位小级徽章 72 级 / 皮肤 24 套；成就清单按到货文件名反向对齐（36 条，见 `f83a852`/`daf1f66`）；**方案①落地** —— `e2e/compress-assets.py` 压成展示尺寸（原图 28.3MB → 端上 580KB），原图移出包，整包 **32.5MB → 1.47MB**，`check-assets.js` 转为阻断护栏 |
| （本轮） | **UI 修复：按钮文字贴顶**（`1d25e46`）：根因是原生 `button` 默认 `line-height: 2.55` + `button.btn{line-height:inherit}`；改全局 flex 垂直居中，并在 `check-wxss.js` 加两条静态护栏 |
| （本轮） | **华容道玩法规划（待确认）**：`docs/华容道玩法规划-待确认.md` —— 参考项目 30 关全部穷举校验（含经典横刀立马 90 步），定位为 A 类固定关卡、独立入口、零图片素材 |
| （本轮） | **字母射击战士放大**（用户反馈「战士太小」）：canvas 上的战士 emoji **30px → 56px**（占画布高 6% → 11%）、底座 60×18 → 104×22；新增**皮肤主色脚底光圈**与**待机呼吸**；站位改由纯函数 `warriorLayout()` 统一算；新增 `renderer-warrior.test.js`（4 用例/57 断言）护栏，防止再被改小 |
| （本轮） | **华容道玩法落地上线**：`game/klotski.js` 引擎 + 30 关关卡库（生成器重算最优解交叉校验）+ `pages/klotski/` 页面（深色棋盘/暖色棋子、零图片）+ 玩法 tab（13→14 款）；单测 21 用例/729 断言 + 端到端 39 条断言 |
| （本轮） | **需求⑤ 皮肤上线**：24 套战士皮肤真图（头像页图片优先 + 对局 canvas 画真图）、前后端目录对齐、生产自愈；线上皮肤 **8 → 23 条**（5 套里程碑等一条 DDL）。含两条硬护栏：图必须真实存在、前后端目录必须一致 |
| （本轮） | **2048 挑战模式**：不限步数冲 2048、只记最少步数（不写星级），新增 `verify-g2048.js` 20 条断言 |
| （本轮） | **需求⑥ 视觉统一（第一期）**：`app.wxss` 抽共享 `.g-head/.g-title/.g-chip/.hud/.hud-chip`，6 个老玩法页统一头部与 HUD 并删掉各自重复定义 |
| （本轮） | **待办 F 死代码清理**：删 `utils/rank.js` + 单测 + `server/seeders/rank-seed.js`，`db.js` 不再 seed ranks |
| （本轮） | **挑战主线 P2**：6 款玩法轮换（每款 5 关）、消消乐/成语/贪吃蛇接入（参数 + 固定种子）、
新增 `utils/play-report.js` 让 8 款玩法统一上报成绩（玩法进度榜终于有全量数据）、启用 4 张玩法成就图标（40 条成就）、
顺手修掉消消乐「取词把 w1 当分组 key → 词卡出现带 * 挖空词」的真 bug |
| （本轮） | **挑战主线 P3**：每 10 关一个里程碑宝箱（+10/+20/+30 星、幂等、按学段隔离、登录同步段位）+ 关卡页宝箱三态卡；
顺带修掉「`unlockType='level'` 皮肤永远解锁不了」的服务端缺陷，并加前后端解锁契约护栏 |

> 顺带修掉一个 E2E 稳定性问题：`verify-challenge` 里「进对局页之后再 reLaunch 走」会稳定挂住开发者工具的自动化通道
> （实测可复现：timeout / page destroyed）。已把「点继续挑战进对局」挪到用例最后一段，跑完不再切路由，48/48 恢复稳定。

> 又修掉一个 E2E 稳定性问题（2026-09-12）：上一轮验证被中断时工具会停在中途页面，`cli auto` 只复用不重开，
> 导致整轮里第一个阶段读到遗留页面报**假失败**并把后续阶段带崩（本轮真实踩到：15 阶段 5 个失败，逐个单跑全通过）。
> 现在工具路径/cli 收敛到 `e2e/lib/devtools.js`，`run-all.js` 会在首个 E2E 阶段前 `quit` 一次（冷却 5s）拉起干净工具，
> `cli auto` 失败自动重试一次；`--no-reset` 可跳过，换机器用 `WX_DEVTOOLS_DIR` 指路径。

> 验证口径：`node e2e/run-all.js` 全量 **15/15 通过**（542s），含挑战主线 73/73、贪吃蛇 52/52、
> 算 24 点 29/29、连连看 26/26、单词闯关 33/33、结算 9/9、18 页 + 8 页渲染回归全绿；
> 单元测试 **34 个文件**（本轮新增 challenge-rewards / avatar-unlock 两套）。

### 挑战主线（P1 已落地，后续分期）

- ✅ **P2 已完成（2026-09-12）**：6 款轮换（射击→消消乐→拼词→连连看→成语→贪吃蛇，每款 5 关）、
  三款新玩法支持参数与固定种子、新增 `utils/play-report.js` 统一上报各玩法成绩、
  启用 4 张玩法成就图标（成就 36→40）、老存档迁移改为按序号平移。
- ✅ **P2 遗留：终关 Boss —— 已按用户拍板的方案 A 落地（2026-09-12）**。
  每学段第 30 关固定为**字母射击 Boss 关**，参数 **15 题 + 7 命**（普通关 10 题 + 5 命），星级阈值不动。
  关卡页金色描边 + `BOSS` 徽标，首页继续挑战卡片前缀 `👑 BOSS`。

  下面是当初「为什么不能直接加题量/减命数」的推导，留档备查
  （口径：通关必须答对 `total-(lives-1)` 题，由 `challenge.starReachability` 计算）：

  | 玩法口径 | 参数 | 通关最低正确率 | 1★ 档（阈值 60%） |
  |---|---|---|---|
  | 字母射击（现状 10 题 / 5 命） | 10 / 5 | **60%** | 恰好可达（已是上限） |
  | 加题量到 15 题、命数不变 | 15 / 5 | **73%** | ❌ 不可达（只能拿 2★/3★） |
  | 减命数到 4 命、题量不变 | 10 / 4 | **67%** | ❌ 不可达 |
  | 加题量 + 命数同比例放大 | 15 / 7 | **60%** | ✅ 可达 |

  ✅ 已选**方案 A**（题量 ×1.5 + 命数同比例放大），实测三档全可达；B/C 未采用：
  方案 B（限时）要新做计时 UI 与超时判定，方案 C（取更难的词）要给词库加难度标注，工作量都更大。
  护栏同时把不可行的老方案钉住了：`starReachability(15,5)` 与 `starReachability(10,4)` 都必须只是 `[2,3]`。

  ⚠️ 另注：`link`/`match`（连连看、消消乐）棋盘是固定 4×4=8 对，**加不了题量**；且它们按「剩余命 3/2/1」给星，
  减命数会让 3★ 档直接消失。这两款要做 Boss 只能走方案 B/C，当前不做。

  ⚠️ 端到端编排坑（踩过一次）：**一条用例只允许加载一次对局页、且必须是最后一步** ——
  先跑第 1 关对局再 reLaunch 到 Boss 对局会稳定 timeout。现改为「前 29 关满星 → 继续挑战直接进 Boss」。
- ✅ **P3 已完成（2026-09-12）**：每通 10/20/30 关各一个宝箱（+10/+20/+30 星，幂等领取、按学段隔离、
  登录时同步段位）—— 直接改善「段位中间级没收益」；学段皮肤（学者之王）走服务端
  `unlockType='level'`（累计通关 30 次）✓ 并修掉了它原来永远解锁不了的服务端缺陷。
  遗留取舍：游客领取只记本地、登录后不补记段位（要补发需把宝箱记录搬到服务端，当前不做）。

### 需求② 错题本（已完成，遗留 1 项）

- ✅ 分页（scope/page/pageSize，兼容老客户端）、删除接口（幂等）、复习答对后「删除/保留」、列表手动移除。
- ⏳ 遗留：**错题本 UI 未做真机/模拟器端到端**（模拟器是游客态没有 openid，且 automator 不支持
  mock `wx.cloud.callContainer`）。目前靠「服务端逻辑单测 + 列表页逻辑单测 + 冒烟里的真实接口链路」
  三层覆盖；等有真机登录态时可以补一次人工验收。

### 需求④ 成就（已完成，遗留 1 项）

- ✅ 6 → **40 条**成就（7 分类）、服务端统一判定并幂等落库、列表带进度与解锁时间、成就页分类筛选 + 进度条。
- ✅ ~~「各玩法首次/深度通关」类成就暂时做不了~~ → **已解决**：P2 让 8 款玩法统一上报 `game_type`
  （`utils/play-report.js`），随即启用闲置的 4 张玩法成就图 ——
  **贪吃蛇大师 / 24 点高手 / 数独行家 / 记忆大师**（各需通关 3 次），成就总数 36 → 40。
  护栏：`achievements.test.js` 的「无死成就」用例会拦住「定义了但永远解不开」的成就。
- ⏳ **遗留（唯一一条，等美术）**：`custom_level_1 出题人` / `custom_level_5 题库作者` 两张图标还没到货，
  现在用 emoji 兜底（实测：40 条成就 / 38 张图标，缺的就是这两张，没有多余的闲置图）。
  素材到了丢进 `miniprogram/assets/achievements/<achievementId>.png` 即可，无需改代码。

### ✅ 素材包体：已按方案①落地（2026-09-12）

- 原图移入仓库根 `assets-src/`（不参与打包、不入库）；端上只留展示尺寸：
  成就 128px / 段位徽章 128px / 皮肤 256px，统一走调色板 PNG。
- 一条命令可重跑：`python e2e/compress-assets.py`；实测 **原图 28.3MB → 端上 580.8KB**。
- 整包 **32.5MB → 1.47MB**（主包限 2MB）✓；`e2e/check-assets.js` 已转成**阻断**
  （整包 ≤1.8MB、单张图 ≤60KB），并排除 `utils/__tests__`（单测目录原本也被打进包）。

---

## 二、待办清单（按建议顺序）

### ⑦ 算 24 点交互改版 ✅ 已完成（`fcd98b4`，保留以下记录备查）

**用户原话**：「按照两个选中的数运算之后就合并运算结果，运算结果为选中的」。

**需求理解**（改成"合并式"玩法，不再拼算式）：
1. 点选**两个**数字牌（各一张）；
2. 点一个运算符（+ − × ÷）→ **立即计算**这两个数的结果；
3. 两张牌**合并成一张结果牌**（4 张 → 3 张 → 2 张 → 1 张），且**这个结果自动成为"已选中"状态**，
   玩家只需再点一个数字牌 + 运算符即可继续；
4. 只剩一张牌且等于 24 → 过关。

**与现状的差别**：现在 `pages/math24/math24.js` 是「拼算式」式——点数字与运算符/括号拼出字符串
（`expr`），再点「＝ 校验」用 `game/math24.js` 的 `evaluateExpr` 精确求值。
新交互**不需要表达式字符串、不需要括号**（每步只做一次二元运算，天然没有优先级问题），
也不再需要「退格 / 清空」——改成「撤销一步」更贴切。

**实现要点（供开工参考）**：
- 复用 `game/math24.js` 现成的精确有理数运算 `add/sub/mul/div` 与 `frac()`，
  合并时按分数存值，避免浮点误差（如 `8 ÷ (3 − 8 ÷ 3)`）；
- 牌的状态从 `{num, used}` 改为 `{ value: frac, label: 显示文本 }`，合并即删两张、插一张；
- 除数为 0 视为非法操作（沿用原逻辑拒绝并提示）；
- 题库与关卡表（`data/math24-levels.js`）**不用动**，只改交互层；
- 单测补「合并链路」纯函数：选中两张 → 运算 → 牌数 −1 且结果被自动选中；除零拒绝；
  最后一张等于 24 判胜。

**需要用户确认的三点**：
1. **卡住/算错怎么办**：合并式玩法可能走进死路（剩下的数再也凑不出 24）。
   建议提供「撤销一步」+「重来本关」，**不设命数**（纯数学解谜，惩罚意义不大）；
2. **是否保留原「拼算式」模式**（比如作为进阶模式），还是**完全替换**？默认按完全替换做；
3. **要不要「提示一步」**：可用 `hasSolution` 反推下一步该合并哪两张，作为卡关兜底。

### A. demo 玩法落地 —— ✅ 6/6 全部完成（2026-09-12 补记）
每款的固定做法：**引擎模块（`miniprogram/game/xxx.js`，纯逻辑可单测）+ 页面四件套 + 注册 `app.json` 与玩法 tab + 单测**。

| # | 玩法 | 来源 demo | 要点 |
|---|---|---|---|
| A1 | 算式天平 | `g5-math-balance.html` | 左盘算式、右盘挑数字使平衡；加减→乘法→混合升档；**数字块要与目标同量级**避免一眼看穿 |
| A2 | 字母拼词工坊 | `g2-word-builder.html` | 给中文释义拼出单词；字母块打乱、可撤回；可接词库（`utils/dict.js`） |
| A3 | 成语拼字 | `g3-idiom-build.html` | 给释义从含干扰字的字块拼四字成语；可接词库的成语分类 |
| A4 | 一笔画 | `g8-one-stroke.html` | **需要关卡生成器**：按欧拉路径条件（奇点 0 或 2 个）生成图形，保证每关都能一笔画完 |

### B. 需求 ② 错题本优化
- `GET /api/wrong/list` 加 `page`/`pageSize` 分页（**不传参数时保持原行为**，向后兼容）
- 新增删除接口（如 `POST /api/wrong/remove`，校验记录属于当前 openid，**幂等**）
- 前端：错题本分页 + 空态；复习答对后弹「删除该错题（已掌握）/ 保留（过段时间再复习）」
- 单测：分页计算边界、删除后列表与统计的变化

### C. 需求 ④ 成就扩充
- 成就扩到 30+：答题（累计答对/单局满分/最高连击）、关卡（通关数/某学段全三星/累计星）、
  玩法（各玩法首次与深度通关）、习惯（连续签到/每日一题）、错题（掌握数/复习次数）、段位
- 服务端统一判定并落库，**幂等**；`/api/achievement/list` 返回进度与解锁时间
- 前端成就页：分类筛选 + 进度条 + 两态

### D. 需求 ⑤ 皮肤扩充 + 主题 + 动态展示
- ✅ **战士皮肤已上线 24 套**（真图 + 主色 + emoji 兜底；头像页图片优先、对局 canvas 画真图）
- ✅ 主色（themeColor）已随皮肤生效：对局战士脚下光圈、怪兽卡片底色都取当前皮肤主色
- ✅ **遗留 A：怪兽美术已到货并接入（2026-09-12）** —— 4 套真图（`assets/skins/monster_01~04.png`，
  单张 12.7~16.6KB）+ 对局内画在题目卡片后面「举着卡片」，形象页也自动显示真图。
  处理流程见 `e2e/prepare-monsters.py`（原图是白底拼图，脚本负责切图/抠底/裁头肩/压缩）。
  顺带修掉 `getMonsterSkin()` 漏返回 `image` 字段的真 bug（对局里一直退回 emoji），
  并补了三条皮肤目录契约护栏 + 一条端到端「真图真的被 canvas 解码」断言。
- ✅ **遗留 B：皮肤动态展示（对局内）已完成** —— 战士**开火后坐力 + 炮口闪光**（2026-09-12）：
  效果由「炮弹离炮口的距离」驱动而**不用时间戳**（小程序 canvas 的 rAF 时间戳在模拟器/后台会节流甚至停摆，
  按距离算与游戏状态严格同步，`_testStep` 手动步进也能复现）；后坐力只压战士本体、底座不动，
  强度与皮肤主色联动（谁换皮肤闪光就是谁的色）。护栏：`renderer-warrior.test.js` 8 用例/171 断言（含后坐力单调衰减、有界、不把头顶压出画布），
  端到端 `verify-game.js` 增至 36 条断言（真实点击开火 → 立刻读强度 → 步进到炮弹飞远 → 归零）。
- ⏳ **遗留 C：里程碑 5 套皮肤要一条 DDL**（见「四、已知问题」第 0 条）。

### E. 需求 ⑥ 老玩法页视觉统一
数独 / 2048 / 算 24 点 / 连连看 / 贪吃蛇 / 消消乐 这几页观感偏单薄，
按新页面（口算冲刺的暖橙大字号、记忆矩阵的深蓝发光格）的风格统一：卡片层次、配色、动效、大触控区。
- ✅ **第一期已完成**：头部与 HUD 抽成共享类并六页统一（删掉各自重复定义）。
- ⏳ **第二期待做（需要你在真机上看效果再定方向）**：棋盘/卡牌本身的层次与动效
  （数独格、2048 方块、24 点牌面、连连看牌背、贪吃蛇格子、消消乐卡面）。
  说明：本机模拟器截图接口会挂（多次实测超时），纯视觉迭代我看不到效果，所以留到你能过目时再做。

### F. 小项
- ~~**图片/音频单文件 ≤200KB**~~：✅ 已按用户要求落成硬性校验（`e2e/check-assets.js` 增加音频扫描 +
  200KB 硬限，60KB 改为只提醒；护栏 `assets-limit.test.js`；规则写进 `AGENTS.md` 第 6 节）。
  实测现包图片 150 张 / 最大 9.3KB / 合计 0.61MB，音频 0 个（音效是 WebAudio 合成、发音走 TTS 插件）。
- ~~**段位徽章图**~~：✅ 已补 8 枚（`e2e/gen_rank_badges.py` 程序化生成），我的页已展示
- ~~**`cloudbaserc.json`**~~：✅ 已加进 `.gitignore`
- ~~**`ranks` 表与 `rank-seed.js`**~~：✅ 已清理（段位改为按星星现算后不再参与判定）
- ~~**前端 `utils/rank.js`**~~：✅ 已清理（连同 `rank.test.js`；已实测三个文件都不在仓库里了）

### G. 华容道玩法 ✅ **已完成（2026-09-12 落地）**

> 用户原话：「把这个华容道玩法也按我们现在数字游戏的玩法放进去吧，主题风格按我们的来，你来规划下，放进待办事项」。
> 参考实现：`E:\Code\小程序\huarongdao`（单文件网页版，30 关全部穷举校验过）。
> **完整方案与落地实况见 `docs/华容道玩法规划-待确认.md`**（用户「按你的建议来」后已实现）：

- ✅ 引擎 `game/klotski.js` + 关卡库 `data/klotski-levels.js`（30 关，生成器重算最优解交叉校验）
  + 页面 `pages/klotski/`（深色棋盘 + 暖色棋子，零图片）+ 玩法 tab（13 → 14 款）
  + 单测 21 用例/729 断言 + 端到端 39 条断言。
- ⏳ **唯一未闭环**：参考项目素材/逻辑的**版权确认**（关卡是程序化生成 + 穷举校验、引擎按我们规范重写；上线前请确认）。

- **定位**：固定关卡类（A 类）数字游戏，独立入口、**不进挑战主线**，与数独/2048/24 点/一笔画同一套存档与星级；
  玩法 tab 从 13 款 → **14 款**。
- **能直接吃**：5×4 棋盘标准 10 子规则、30 关（8~100 步，含经典「横刀立马」90 步）、
  星级口径（≤1.15×最优 → 3★、≤1.5× → 2★，否则 1★）、提示/演示/撤销/重来。
- **要做的**：① 引擎移植成 `game/klotski.js`（纯逻辑可单测）；② `e2e/gen-klotski-levels.js`
  生成 `data/klotski-levels.js` 并在生成时校验（棋子组合 / 标注步数=求解步数 / 解路径可回放 / 关卡唯一 / 难度递增）；
  ③ `pages/klotski/` 四件套 + 我们的视觉主题（纯 CSS 方块，**零图片**）；④ 拖拽为主（一次连续滑动=1 步）+ 点击兜底；
  ⑤ 两套单测 + 一条端到端（`verify-klotski.js`）+ 结构回归（13→14 款）。
- **工作量**：P1 引擎+数据 1 天 → P2 页面+主题+接入 1~1.5 天 → P3 端到端+动画音效 0.5~1 天。

---

## 三、待用户拍板的事项

| # | 事项 | 我的建议 |
|---|---|---|
| 1 | **2048 目标上限收到 512** 是否认可？ | 认可；若要保留「打到 2048」的成就感，把它做成不限步数的挑战模式（记录最佳步数） |
| 2 | ~~「累计星星」口径~~ | ✅ 已拍板：**按我的页（云端口径）**，已实现 |
| 3 | ~~段位徽章图~~ | ✅ 已拍板：**要补**，已补 8 枚并用上 |
| 4 | ~~玩法落地顺序~~ | ✅ 已拍板：按 天平→拼词→成语→一笔画（4 款已全部交付） |
| 5 | ~~华容道：棋子用哪套皮~~ | ✅ 已按建议：保留经典三国名，视觉用我们的配色 |
| 6 | ~~华容道：是否保留「一键演示最优解」~~ | ✅ 已按建议：保留，且**演示通关不计星、不写进度** |
| 7 | ~~华容道：关卡只做 30 关还是要扩~~ | ✅ 已按建议：先上 30 关；生成器可随时扩 |
| 8 | **华容道：参考项目素材/逻辑的版权** | ⏳ **仍需你确认**（关卡是程序化生成+穷举校验、引擎已按我们规范重写；上线前请拍板） |
| 9 | ~~华容道：在玩法 tab 里的位置~~ | ✅ 已按建议：放「智力」分组，与一笔画、数独相邻 |
| 10 | ~~素材包体方案~~ | ✅ 已拍板：方案①（展示尺寸压缩 + 原图移出包），已落地，整包 32.5MB → 1.47MB |
| 11 | ~~终关 Boss 怎么做~~（原「加题量/减命数」已被证明会造出 1★ 死区） | ✅ 已拍板并落地**方案 A**：第 30 关固定字母射击 Boss，15 题 + 7 命，三档星级可达；关卡页 BOSS 徽标 + 首页 `👑 BOSS` 前缀 |

---

## 四、已知问题与运维注意

0. **✅ 线上 DDL 已执行完毕（2026-09-12）**：生产库 `avatars.unlockType` 原本是**早期 ENUM**、缺 `milestone`，
   导致 5 套里程碑皮肤插入时报 `Data truncated for column 'unlockType'`，线上只有 23 条。
   用户执行了这条 SQL 后**已恢复正常**：
   ```sql
   ALTER TABLE avatars
     MODIFY COLUMN unlockType ENUM('stars','rank','level','free','milestone')
     NOT NULL DEFAULT 'free';
   ```
   验证证据（执行后立刻实测）：
   - `GET /api/health` → `avatars: 28`、`roster: { ok: true, total: 28, created: 5, failed: [] }`
     —— 服务端自愈把缺的 5 行自己补进去了，不再需要人工插数据；
   - `GET /api/avatar/list` → **战士 24 + 怪兽 4 = 28**，5 套 `milestone_*` 都在列表里；
   - 里程碑皮肤仍**禁止手动解锁**：`POST /api/avatar/unlock {avatarId:'milestone_30'}` 返回
     `4002 该皮肤需每日一题连续签到达到天数后自动解锁`（符合设计，不是 bug）；
   - `e2e/smoke-api.js` 的皮肤目录下限已从 19 调回 **24 张战士**，并新增「`milestone_30`/`milestone_365`
     必须在列表里」两条断言，冒烟 14/14 通过。

   ⚠️ 顺带发现一个**不是 bug 但容易误判**的历史遗留：`server/seeders/avatar-seed.js` 里 12 条的 `icon`
   指向 `/assets/avatars/*.png`，而包内**没有这个目录**（只有 achievements/ranks/skins/tab）。
   原因与现状：端上从头到尾**不用这个字段** —— `pages/avatar` 是拿 `utils/skins.js` 的本地图渲染的
   （`avatar.js` 开头注释写明了「后端 icon 素材暂缺、改用本地 emoji/图片占位」），怪兽本来就只有 emoji。
   所以没有空图问题；但**别贸然去"修"这个字段**：`ensureAvatarRoster` 用的是 `findOrCreate`，
   改 seed 不会更新线上已有行，改了也只在全新环境生效。

1. **云托管 Git 自动部署链路当前是坏的**：构建阶段报 `blob unknown to registry`（连续两次），
   属平台构建侧问题。已改用 CLI 部署绕过：
   ```
   cd study_game
   cloudbase cloudrun deploy -e prod-d6gnifjoe28cfd96f -s express-g0hk --source . --port 80
   # 交互会问「About to start deployment」→ y；「Enable gray deployment」→ 直接回车（选 No）
   ```
   建议在云托管控制台点一次「重新构建」确认平台恢复。
   （2026-09-12 晚：CLI 部署 `38f5d35` 一次成功，构建约 2 分钟；线上冒烟 14/14 通过。
   并已实测生产环境 `level` 解锁分支真的生效：把测试 openid 的累计通关数刷到 30 以上后
   `POST /api/avatar/unlock {avatarId:'skin-scholar-king'}` 返回 `code:0` —— 修复前这条永远返回 4002。）
2. **生产库曾缺列**：`scores.game_type` / `scores.type_key`（已由用户手工执行 SQL 补齐）。
   今后改 `server/models/` 务必把「生产库 DDL」列入发布清单。
3. **子代理任务无法送达**：本环境下 `spawn_agent` / `followup_task` 的任务正文传不到子代理
   （对方只收到空任务并回复"请告诉我任务"），**并行子代理暂不可用**，请串行推进。
4. **PowerShell 改文件会毁掉中文**：PS5.1 默认按 GBK 读写 UTF-8 文件，
   一律用 `apply_patch` 改代码（已踩坑两次）。

---

## 五、如何恢复工作

```bash
cd study_game

# 分层验证（改什么跑什么，别每次都跑全量）
node e2e/run-all.js --no-e2e    # 静态 + 单元，约 10 秒 —— 改数据/后端/文档时跑这个
node e2e/run-all.js             # 全量 15 阶段，约 9 分钟 —— 只在改前端页面/交互时跑
node e2e/run-all.js --only=game # 单点排障（--only 只吃一个 id）
node e2e/run-all.js --no-reset  # 跳过「开跑前重启开发者工具」；换机器用 WX_DEVTOOLS_DIR 指路径
node e2e/smoke-api.js           # 线上接口冒烟（部署后跑）

# 全量 15 个阶段的 id（按执行顺序）：
#   syntax structure wxss assets unit
#   game result math24 link snake klotski g2048 challenge pages m2m4

# 关卡/题库相关工具
node e2e/gen-math24-levels.js   # 重新生成算 24 点题库（穷举 1~13 全组合 + 求解器校验）

# 玩法备选 demo（可视化稿，浏览器打开）
#   demo/playground/index.html    —— 8 款玩法 demo
#   demo/playground/redesign.html —— 关卡与玩法重构可视稿
```

### 提交约定
- 编辑一律用 `apply_patch`；提交信息用中文、分点说明"做了什么/为什么/怎么验证"。
- 提交前必须验证通过；`docs/*` 除本文件外按约定只本地更新。
