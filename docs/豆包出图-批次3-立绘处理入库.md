# 处理请求 · 立绘统一裁边压缩并放入 CDN 目录（2026-09-19）

## 这一轮只做一件事

把 **40 张**对局立绘统一做「裁掉透明边 + 压到合适尺寸 + PNG 优化」，
输出到 CDN 目录 `E:\Code\小程序\study_game\art\skins\`。

**这是纯图像处理任务，不需要重新生成任何图片。**

## 输入源（只读，禁止修改/删除/移动）

**A. 本轮新出的 12 张**（512×512，已确认真透明底）：

    E:\Code\小程序\study_game\assets-src\skins\boss_pixel_devourer.png
    E:\Code\小程序\study_game\assets-src\skins\boss_jade_dragon.png
    E:\Code\小程序\study_game\assets-src\skins\boss_crimson_ogre.png
    E:\Code\小程序\study_game\assets-src\skins\boss_abyss_kraken.png
    E:\Code\小程序\study_game\assets-src\skins\boss_mech_lord.png
    E:\Code\小程序\study_game\assets-src\skins\boss_magma_titan.png
    E:\Code\小程序\study_game\assets-src\skins\skin-flame-berserker.png
    E:\Code\小程序\study_game\assets-src\skins\skin-tide-druid.png
    E:\Code\小程序\study_game\assets-src\skins\skin-gale-ninja.png
    E:\Code\小程序\study_game\assets-src\skins\skin-thunder-lancer.png
    E:\Code\小程序\study_game\assets-src\skins\skin-radiant-paladin.png
    E:\Code\小程序\study_game\assets-src\skins\skin-void-assassin.png

**B. 已有的 28 张**（在打包目录里，**只读取，绝对不要修改或删除**）：

    E:\Code\小程序\study_game\miniprogram\assets\skins\  目录下的全部 .png（28 张）

> ⚠️ 特别注意：`miniprogram\` 是小程序的打包目录。你**只可以从它读取图片**，
> **绝对不要修改、重命名、删除、移动里面的任何文件**。
> 也不要动 `E:\Code\小程序\study_game\assets-src\skins-orig\` 里的备份。

## 处理规则（40 张一律照做）

1. **按 alpha 通道求包围盒，裁掉四周透明边**，四周保留约 2% 安全边距（避免贴边被切）；
2. **最长边缩放到不超过 512 像素**：大于 512 才缩，**小于 512 的不要放大**（放大只会变糊）；
3. **保持真透明底**（alpha 通道），不要加白底/底色；
4. PNG 优化：**调色板量化到不超过 256 色**（保留透明通道），并用最高压缩级别保存；
   目标单张 **≤150KB**（卡通扁平色很好压；压不到也别牺牲清晰度，但请把体积报给我）；
5. 文件名**沿用原文件名**（不要改大小写、不要加后缀）。

## 输出

    目录：E:\Code\小程序\study_game\art\skins\     （目录若不存在请创建）

40 个文件，文件名与输入一一对应，例如：

    E:\Code\小程序\study_game\art\skins\boss_jade_dragon.png
    E:\Code\小程序\study_game\art\skins\skin-recruit.png

## 硬性约束

- **只写 `art\skins\` 这一个目录**；除它以外任何文件都不要新建、改写、删除；
- 不要动 `miniprogram\`、`assets-src\`、`art\achievements\`、`art\ranks\`；
- 不要联网、不要重新生成图片。

## 做完回报什么

回报一张清单：**每个文件的 名称 + 尺寸 + 体积KB + 是否真透明底**，以及 40 张的**总体积**。
另外告诉我有没有哪张没处理成功。不需要做其它任何事。
