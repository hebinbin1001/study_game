# 当前未完成事项（截至 2026-09-13）

---

## 【2026-09-19 收尾】本轮做完的

| 事项 | 结果 |
| --- | --- |
| 字母射击改版 | canvas 引擎 → WXML/CSS + 纯逻辑 `game/shoot.js`；词槽 + 字母面板 + Boss 血条 + 打错反击 |
| 题库（新） | 学习 tab 入口；按学段浏览/搜索/编辑/新增/删除；**自建库挂学段作附加题源**；掌握状态（✓/✗ + 薄弱/已掌握筛选） |
| 排行榜玩法榜 | 修掉撑破页面的平铺 14 个 chips；改为「卡片总览 → 详情榜」；后端加 `/api/ranklist/progress-summary` |
| 未注册不上榜 | 总榜/玩法进度榜/玩法总览/我的名次四处统一「昵称非空」门槛 |
| 微信名采集 | 昵称框点一次「使用微信昵称」即同时记下微信名（独立字段，不再被游戏昵称覆盖）；「我的」页补录条 |
| 「下一关」提示 | 8 款玩法 + 字母射击结算页；`challenge.nextLevelUrl` 统一口径 |
| 词卡大小写 | 消消乐/连连连看不再整体大写 |
| 自定义题库 → 自建关卡 | 改名 + 编辑器接题库选题（`utils/level-picker.js`） |

## 【2026-09-19 深夜批次】又做完的

| 事项 | 结果 |
| --- | --- |
| 头像换不了 | 查明两个原因：① chooseAvatar 给的是微信**临时路径**，直接存库 → 本机重启失效、别人裂图（已改为压缩上传 + 稳定地址 + `avatar_blobs` 表）；② 与昵称同属隐私接口，需在公众平台声明**头像**（用户已执行） |
| 限时抢答 | 新增第 7 条玩法线（倒计时抢词，答错扣 2 秒）；难度用时间做（60→30 秒），不进主线的 30 关轮换 |
| 12 款新立绘 | 豆包出图 6 Boss + 6 元素战士（512×512 真透明底），统一裁边压缩后进 `art/skins/` |
| 立绘 CDN 迁移 | 包内只留默认战士 + 默认怪物两张兜底，其余 38 张走云托管 `/assets/**`；**包内图片 184KB → 20KB** |
| 皮肤目录 | 战士 24→30 套、怪兽 4→10 套，前后端逐条对齐（有 parity 护栏）；线上已生效 |

## 【2026-09-19】还没做的（下次接着做）

1. **上传新版本**（用户操作）：本轮改了 `app.json`、多个页面结构、素材（立绘走 CDN），
   **上传前必须重开开发者工具项目**。
2. **真机复验**：对局立绘大小（新 512 图是否够清晰）、头像更换、限时抢答、题库页、排行榜两层、
   微信名采集、首页「完善昵称」引导条。
3. **自建关卡的后续优化（可选）**：现在是「10 题一关」硬口径；若要做 20 题长关卡或
   「一键用我的题库自动生成一关」，需改后端 10 题强校验。
4. **二期候补**：配对类（消消乐/连连看）用「多轮 / 限时」做难度；数字智力是否升第 5 个底部 tab。

## 环境备忘

- 生产库已建表：`word_entries` / `word_banks`（DDL 见 `docs/题库-生产库建表与部署说明.md`）。
- 存量用户资料已按用户要求清空（轻量版 `UPDATE users SET nickname=''...`），需要通过首页「完善昵称」引导条重新采集。
- 管理端 `ADMIN_OPENIDS` 白名单已配（openid 判定生效）；管理页能看到用户微信名。

> 结论先说：**代码侧待办已清零**。下面 5 条都需要你参与（提供内容 / 拍板 / 真机验证），
> 或者是主动留到二期的设计项。已闭环的部分列在最后，避免重复排查。

## 一、需要你提供内容（唯一真正「没做」的功能性事项）

1. ~~**协议要补的三样信息**~~ ✅ **已完成（2026-09-13）**：用户提供主体名称「追梦」、
   微信 `h842917647`、邮箱 `842917647@qq.com`，响应时限按要求填「收到申请后 15 个工作日内处理并回复」；
   已补进 `pages/agreement`：用户协议新增第 8 条「运营主体与联系方式」、隐私政策第 7 条改为具体联系方式、
   页脚加一行主体信息。

## 二、需要你决定做不做

2. ~~**青少年模式 / 防沉迷**~~ ❌ **用户 2026-09-13 明确不做**（教育类小程序，平台不强制）。

## 三、需要你验证（真机）

3. **真机复验 11 项** —— 清单在 `docs/待办收尾与上线前清单.md` 第二节，重点是：
   数字智力先进关卡页（口算/天平按年级、2048 每关最快用时）、一笔画滑动、24 点先点运算符、
   贪吃蛇贴边转向、错题本三档与「继续复习」、段位详情 72 级、排行榜头像+段位徽章+**玩法进度榜 14 款**、
   首页动态推荐。上传前记得在开发者工具**重开一次项目**。

## 四、主动留到二期的设计项（不是漏做，等你排优先级）

4. **配对类玩法（消消乐 / 连连看）的难度**：一期刻意没加难度（4×4 版式 + 剩余命折星，加牌/减命都会破口径），
   二期用「多轮 / 限时」做。
5. **数字智力类是否升成第 5 个底部 tab**：视内容厚度再定；升级只需补两枚 tab 图标。

## 五、已闭环（供对照）

- **排行榜玩法进度榜**：14 款玩法全部有成绩上报；前端 `loadGame()` 不再写死 `game=word_warrior`；
  新增静态护栏 `e2e/check-reports.js`（以后新增玩法忘上报会判红）。
- **段位曲线与星星口径**：升级成本改为每 3 级 +1 星（满级 527 → 947 星）；段位星改为
  「按 玩法×学段×题型×关卡 取历史最高星去重求和」；补上一直缺失的 `POST /api/rank/sync`
  （并修了按 `openid` 查 scores 表导致 5000 的 bug，线上冒烟已验证 14/14 通过）。
- **签到奖励重标**（2026-09-13，配合段位曲线 C）：旧口径每天 10 星 + 里程碑 30/100/200/500，
  一个月满勤 ≈ 1090 星 > 满级 947 星，光签到就能满级。改为「每天 +1 星；连续正好踩到
  3/7/14/30 天当天额外一次性 +2/+5/+8/+15 星」→ 一个月满勤 60 星、一年满勤约 725 星。
  口径落在 `server/checkin-rewards.js`（纯函数 + 单测），打卡入口（每日一题 `daily.js`）与
  签到页 `checkin.js` 统一改为**重算段位星**而不是直接 `stars +=`（否则会被下一次重算覆盖）。
- **第三批 9 条反馈**：排行榜头像/大段徽章、一笔画滑动 + 34 关、数字智力统一关卡页
  （含口算/天平按年级分档、记忆矩阵按格子×数字位数、2048 扩到 14 关 + 每关最快用时）、
  段位详情页（72 级门槛 + 距下一段还差多少星）、玩法线内页标题带玩法名。
- **更早批次**：「已复习」口径、登录门禁、结算页游客提示、分享补齐、点击响应护栏、
  玩法线（6 款题库玩法各 30 关 + 玩法页二级分段）。
- **你已确认/执行的**：华容道版权保持现状；云托管 push 即自动部署；里程碑 5 套皮肤的 DDL。
- **测试侧**：snake E2E 的偶发假失败已修（绝对值断言 → 增量断言），之后全量 19/19 通过。
