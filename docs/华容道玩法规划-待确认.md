# 华容道玩法落地规划（**已按建议落地**）

> 用户 2026-09-12：「把这个华容道玩法也按我们现在数字游戏的玩法放进去吧，主题风格按我们的来，你来规划下，放进待办事项」。
> 参考实现：`E:\Code\小程序\huarongdao`（单文件网页版，30 关）。
> 2026-09-12 用户回复「一个个按你的建议落地吧」→ 第 7 节的 5 条全部按建议执行，**已实现并跑通全量验证**（见第 8 节落地实况）。

---

## 一、参考实现盘点（可以直接吃什么）

| 项 | 现状 |
|---|---|
| 形态 | 单文件网页 `华容道.html`（零依赖，双击即玩）；逻辑在 `src/game.template.html` 的 `<script id="core">`（≈210 行）+ `<script id="ui">`（≈500 行） |
| 棋盘 | 5 行 × 4 列；标准 10 子：曹操 2×2 ×1、竖将 1×2 ×4、横将 2×1 ×1、小兵 1×1 ×4 → 占 18 格，留 2 空 |
| 目标 | 把「曹操」移到棋盘底边中间的 2×2 出口 |
| 计步口径 | **一次连续直线滑动算 1 步**（拖得越远越省步数）；单格口径下「横刀立马」=116 步（与文献经典结论一致，是有力的正确性佐证） |
| 关卡 | **30 关**，全部经**穷举校验**：4×5 全状态空间 53,954 个可达局面、最远 101 步；关卡最少步数 8 → 100 步 |
| 难度分档 | 入门 8 关（8~38 步）、进阶 10 关（42~82）、困难 6 关（85~95）、经典 1 关（横刀立马 90）、炼狱 5 关（96~100） |
| 星级 | 实际步数 / 理论最少步数：≤1.15 → 3★、≤1.5 → 2★、否则 1★ |
| 已有功能 | 拖拽移动、撤销、重来、**提示**（按当前局面实时求最优走法）、**自动演示**（按最优解走完）、进度存 localStorage、解锁全部关卡 |
| 质量 | 核心测试 1837 条断言（关卡合法性 / 求解步数与标注一致 / 解路径独立回放验证）；浏览器 UI 测试 27 条 |
| 关卡生产链 | `tools/`：gen（固定种子随机 + 求解）→ hardest（反向全图 BFS 求最难局面）→ curate（按难度曲线挑选 + 相似度过滤）→ build（注入模板） |

**结论**：核心规则、关卡数据、难度曲线、星级口径都是现成且高质量的，**我们主要是「移植 + 换皮 + 接进度体系」**，不需要重新设计玩法。

---

## 二、在我们小程序里怎么放（定位）

按此前拍板：**固定关卡类（A 类）不进挑战主线，保持独立入口**。华容道属于纯智力数字游戏，因此：

- 玩法 tab 新增一款「🧩 华容道」（已解锁玩法 13 → **14** 款）；
- 页面内自带**关卡选择**（4 档 30 关，锁/星与他人页面一致：上一关 ≥1 星解锁下一关）；
- 不进挑战主线，也不占用「继续挑战」卡片；
- 与数独 / 2048 / 24 点 / 一笔画 同一套**存档与星级**（`storage.saveStars('klotski', level, stars)`，键形如 `klotski@<level>`）。

---

## 三、移植方案（技术）

### 3.1 引擎：`miniprogram/game/klotski.js`（纯逻辑，可单测）

从参考实现的 `<script id="core">` 移植并改成我们项目风格（CommonJS、纯函数、无 wx 依赖）：

| 函数 | 作用 |
|---|---|
| `parseGrid(rows)` | 关卡字符串网格 → 棋子数组（含 名称/尺寸/位置） |
| `buildOcc(pieces)` | 棋子占位表（含出口判定用） |
| `maxSlide(occ, pieces, i, dx, dy)` | 某子在某方向**最多能滑几格**（0 = 不动） |
| `applyMove(pieces, i, dx, dy, dist)` | 执行一次滑动（返回新棋子数组，不改原数组） |
| `isWin(pieces)` | 曹操是否到达底边中间出口 |
| `packKey / unpackKey` | 局面编码（BFS 去重 + 存档用） |
| `solve(pieces, opts)` | BFS 求最优解（`opts.full=true` 返回完整路径用于「演示」，否则只算首步用于「提示」） |
| `starsFor(minMoves, moves)` | ≤1.15× → 3★、≤1.5× → 2★、否则 1★ |

### 3.2 求解性能：先测，再决定「实时算」还是「预制解」

参考实现是在浏览器里实时 BFS 的。小程序端我们要先量：

1. 在 Node 侧用我们的引擎跑一遍 30 关的 `solve()`，记录耗时（参考项目已验证状态空间 53,954，Node 上应在百毫秒级）；
2. **不论快慢，关卡数据里都随关带一条最优解路径**（生成时算好）——演示直接放，不依赖端上算力；
3. 「提示」策略：玩家仍走在最优解的轨道上 → 直接用预制解的下一步（瞬时）；已经走偏 → 才现算 BFS（并加 300ms 超时保护，超时就提示「按 R 重来可看完整演示」）。

### 3.3 交互：拖拽为主 + 点击兜底

- **拖拽（主）**：`touchstart/touchmove/touchend` 判定主轴方向与位移量 → 滑到该方向尽头（受阻挡为止），**一次拖拽记 1 步**（与参考口径一致，公平性靠它）；
- **点击（兜底）**：点棋子选中 → 点方向键或点目标格滑 1 格（每次记 1 步），方便单手/精细操作；
- 棋盘容器需 `catchtouchmove` 阻止页面滚动（否则拖动会把页面带着滚）。

### 3.4 关卡数据：`miniprogram/data/klotski-levels.js`（构建产物，勿手改）

新增 `e2e/gen-klotski-levels.js`（Node），从参考项目 `tools/levels.json` 读取 30 关，**生成时逐关校验**：

1. 棋子组合合法（恰好 1 曹 + 4 竖将 + 1 横将 + 4 兵 + 2 空）；
2. 用我们的引擎 BFS 求最少步数，**必须等于关卡标注的 `minMoves`**（不一致就报错退出）；
3. 解路径可回放：逐步执行后确实到达出口；
4. 关卡唯一（局面去重）、难度单调递增；
5. 顺带产出每关最优解路径（`solution`）与关卡元信息（名称 / 档位 / 是否经典）。

输出格式与现有 `data/math24-levels.js`、`data/sudoku-levels.js` 对齐，便于统一测试与展示。

---

## 四、页面与视觉（主题按我们的来）

### 4.1 页面：`pages/klotski/`（四件套）

结构（沿用我们其他玩法的骨架）：

```
顶栏：‹ 返回 | 🧩 华容道 | 关卡档位标签
HUD：第 N 关 · 步数 12/最少 8 · ⭐⭐⭐（复用 components/game-hud）
棋盘：5×4 卡片式棋盘（纯 CSS 方块，无图片）
操作区：💡提示 · ↩️撤销 · 🔄重来 · ▶️演示
结算：复用 components/settle-pop（过关星级 / 再来一次 / 下一关）
关卡列表：4 档 30 关，锁/星/当前关
```

### 4.2 视觉（我们的语言，不用参考实现的皮肤）

- 棋盘：深色渐变底板 + 圆角格线（与「记忆矩阵」同一套深蓝发光语言，但走**暖色棋子**以免和数字游戏撞脸）；
- 棋子配色（主色取自我们现有色板）：
  - 曹操 2×2：**主色红金**（`#ff5d8f`→`#ffb703` 渐变 + 金边），最大最显眼；
  - 五将（关/张/赵/马/黄）：蓝紫系（`#4cc9f0` / `#b388ff`），可按竖/横区分深浅；
  - 兵 1×1：绿色（`#7bd389`）；
  - 出口：金色虚线门 + 「出」字水印，曹操贴上去时有呼吸高亮；
- 硬阴影 + 圆角 + 大字号，与首页/结算页一致；拖动时棋子抬起（`translateY + 阴影变大`），落位有 120ms 过渡；
- 棋子命名：**保留经典三国名（曹操/关羽/张飞/赵云/马忠/黄忠/兵）**——玩家认知成本最低，也自带情怀。若你想统一到我们的世界观，可换成「大魔王 / 怪兽小兵 / 战士」一套皮（待确认）。

### 4.3 音效与动效（二期）

- 滑动「嗒」、通关「叮咚」、卡住提示音；演示时逐格动画播放。一期先做静音可用的版本。

---

## 五、测试计划（与我们现有流水线一致）

| 层 | 内容 |
|---|---|
| 单测 1 | `klotski.test.js`：网格解析、滑动距离（含被阻挡为 0）、一次滑动=1 步、撤销、胜负判定、局面编码往返、**星级可达性**（任意通关 ≥1★、最优解必得 3★，防 R1 那类死区） |
| 单测 2 | `klotski-levels.test.js`：30 关结构、棋子组合合法、标注步数 = 求解器步数、解路径可回放通关、关卡唯一、难度递增 |
| 端到端 | `verify-klotski.js`：进页面 → 关卡列表 30 关 → 选第 1 关 → 拖 3 格只记 1 步 → 撤销 → 提示给出下一步 → 按最优解走完 → 结算 3★ → 存档写 `klotski@1` → 第 2 关解锁 |
| 回归 | `structure-check.js` 里「已解锁玩法 13 款」等断言同步改成 14 款；`run-all.js` 注册 klotski 阶段 |
| 包体 | 华容道**零图片**（纯 CSS 方块），只增约 20~30KB JS —— 当前整包 1.47MB，无压力 |

---

## 六、工作量与分期

| 期 | 内容 | 预估 |
|---|---|---|
| P1 | 引擎移植 + 关卡数据管线（含校验）+ 单测两套 | 1 天 |
| P2 | 页面四件套 + 视觉主题 + 拖拽/点击交互 + 进度星级接入 + 玩法 tab 与结构回归 | 1~1.5 天 |
| P3 | 端到端用例 + 演示动画 + 音效 + 关卡解锁全流程验收 | 0.5~1 天 |

---

## 七、拍板结果（2026-09-12，用户「按你的建议来」）

1. **棋子用经典三国名** ✅ —— 曹操 / 关羽 / 张飞 / 赵云 / 马超 / 黄忠 / 兵，视觉用我们的配色。
2. **保留「演示」但演示通关不计星** ✅ —— stars=0、结算星级显示 👀，且**不写进度**（不会解锁下一关）。
3. **先上 30 关** ✅ —— 生成器已支持按难度曲线继续扩，后续按反馈再加。
4. **版权** ⏳ —— 关卡是程序化生成 + 穷举校验（非抄录题集），引擎按我们规范重写；**上线前仍需你确认可用**（唯一未闭环项）。
5. **入口放「智力」分组** ✅ —— 与一笔画、数独相邻（玩法 tab 13 → 14 款）。

---

## 八、落地实况（2026-09-12）

| 层 | 产物 |
|---|---|
| 引擎 | `miniprogram/game/klotski.js`（纯逻辑、无 wx 依赖）：解析/校验、滑动距离、一次滑动=1 步、胜负、局面打包、BFS 求解、回放、星级折算 |
| 关卡库 | `miniprogram/data/klotski-levels.js`（30 关，构建产物）+ 生成器 `e2e/gen-klotski-levels.js`（生成时重算最优解并与标注交叉校验） |
| 页面 | `miniprogram/pages/klotski/`（四件套；深色棋盘 + 暖色棋子 + 硬阴影，**零图片**） |
| 接入 | `app.json` 注册页面、玩法 tab 新增「🧩 华容道」（智力分组）、`structure-check` 同步（已解锁 13→14 款 + 关键页） |
| 测试 | `klotski.test.js`（14 用例 / 66 断言）、`klotski-levels.test.js`（7 用例 / 663 断言）、`verify-klotski.js`（39 条断言） |

**存档键说明**：A 类玩法沿用老约定 `<玩法>_<关卡>`（如 `g2048_1` / `math24_1`），
所以华容道是 **`klotski_1`**（不是 `klotski@1`）。

**四条实测发现（都已在代码或用例里处理）**

1. **`Element.offset()` 只返回 left/top**（没有宽高），要尺寸得用 `element.size()` ——
   一开始用 offset().width 算出 NaN 坐标，拖拽被判成"没动"。
2. **开发者工具会把合成 touch 的坐标固定成 (44,44)**：`element.touchstart({x,y})` 传的坐标不生效，
   所以拖拽用例改为「调用页面真实触摸处理器 + 用例给定坐标」，既不假失败又覆盖了「像素 → 格数」换算。
3. 顺带修掉一个真 bug：通关时把 `playing` 置 false，会让挂在「对局」分支里的结算弹层被一起卸载（现在通关保持 playing=true）。
4. `settle-pop` 是自定义组件，组件内的 `.over-mask` 自动化查不到 → 页面外包了一层 `.k-settle` 供断言。
