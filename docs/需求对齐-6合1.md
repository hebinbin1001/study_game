# 《词力战士》6 合 1 需求文档 × 现状 —— 需求对齐表

> 说明：以“当前已实现”为基线，将《微信小程序需求文档.md》逐节对齐。
> 标记：【已实现】现状已满足或兼容；【适配】需要按现状改造/折中；【新增】尚未实现；
> 【缓做】价值有限/量大，列入远期；【不建议】与现状优势冲突或风险高。
> 本文件是后续开发的需求基线；改需求先改此文件。

## 一、项目概述 / 技术架构
| 文档条目 | 对齐结论 |
|---|---|
| 单词/成语益智闯关小程序 | 【已实现】当前为字母/汉字打怪闯关，三形态+关卡地图 |
| 6 合 1 学习小游戏合集 | 【新增】仅完成 1 套玩法；需按“第二玩法=词义消消乐”起步扩充 |
| 纯本地游戏逻辑、0AI、后端纯数据 | 【已实现】判定/计分全前端；后端只存取（成绩/错题/排行/皮肤等） |
| 微信云托管单服务、MySQL、对象存储静态资源 | 【已实现】云托管+MySQL；静态暂以 emoji/内置为主；对象存储属可选演进 |
| 灰度发布/白名单/回滚 | 【已实现-流程】云托管支持滚动与回滚；“白名单内测”属流程规范，文档化即可 |

## 二、全局统一规则（6 游戏通用）
| 文档条目 | 对齐结论 |
|---|---|
| 难度分级 初/中/高 | 【适配】现状按 7 学段+题型分类更细；可将“难度”=学段+分类，不另建三级 |
| 每局固定生命、答错扣血 | 【已实现】3 ❤、答错/超时扣命 |
| 连击加分倍率递增 | 【适配】现状连击仅提示（不倍率计分，服务端防伪按答对数×10）。若做倍率分需限定为“展示分”或另计连击奖分不违反上报口径 |
| 每局自动记录答错词到错题本 | 【已实现】（登录用户，答题即上报） |
| 结算发金币/积分 | 【新增-需拍板】教育类目注意站内性质（无支付/兑换）；可做“积分=累计星/星星替代展示”或“金币”仅内部 |
| 全局顶部状态栏统一 | 【已实现-需微调】HUD 有❤/得分/题号/声音；缺：连击倍率展示、暂停按钮 |

## 三、六大游戏逐一对齐
| 游戏 | 文档玩法 | 对齐 |
|---|---|---|
| 1 字母射击打怪 | 怪兽残缺词+字母按钮补齐 | 【已实现】（核心；可视为已含：挖空+四选+怪兽卡） |
| 2 词义消消乐 | 棋盘词↔义匹配消除、限时连击 | 【新增·建议】第二玩法首选（复用词库/释义/错题/结算） |
| 3 词语连连看 | 连连看规则连线配对 | 【缓做】玩法引擎独立、量大；排在消消乐之后 |
| 4 单词弹弹球 | 释义小球+单词子弹 | 【缓做】偏动作、与答题内核分离度高 |
| 5 单词贪吃蛇 | 蛇吃正确词 | 【缓做】同上 |
| 6 算 24 点 | 四数四则运算 | 【缓做/不建议】非字词类目；如扩品类再议 |

## 四、UI 规范
| 文档条目 | 对齐结论 |
|---|---|
| 轻卡通明亮低饱和、主色蓝 | 【适配】现状为天空蓝卡通，可保留主色 #4285F4 作为强调，不推翻现有视觉 |
| 大厅：头像/昵称/金币/错题入口 + 2 列玩法卡 + 复习专区 | 【适配】首页已有登录条+入口网格；演进为“玩法大厅（6 格占位→逐个填玩法）+专区” |
| 对局顶部：❤/倒计时+连击/暂停 | 【新增·微调】补暂停与连击倍率展示 |
| 结算弹窗：得分/最高连击/金币/本局错题(可查释义)/再来/返回 | 【适配】结算已有星/分/操作；补“本局错题+释义”与（若做金币）金币行 |
| 错题复习页：词语+释义+错误次数+练习按钮 | 【已实现·可增】已有（词语/熟练度/到期/开始复习）；可加“用其他玩法练习” |

## 五、数据库（4 表）对齐现状（11 表）
| 文档表 | 对齐 |
|---|---|
| user_info（openid/金币/积分/总局数/最后登录） | 【已实现-部分】users 有 openid/nickname/avatar/token/phone；金币/积分/总局数【新增·需拍板】 |
| word_lib（type/content/explain/difficulty） | 【已实现-本地化】词库内置 data/*.js（离线优势），type 8 码≈word/idiom 等；difficulty≈学段。不建议迁云端拉题 |
| user_error（openid/word_id/error_count/last_time） | 【已实现】wrong_records（含艾宾浩斯复习） |
| game_record（openid/game_type/score/combo/coin/time） | 【已实现-部分】scores（grade/level/score/combo/stars/…）；game_type/coin【需拍板】 |

## 六、API 对齐
| 文档 | 现状 |
|---|---|
| /user/info、/user/save、/wordlib/get、/error/save、/error/list | 【已实现-等价】/user/me、/score、内置词库(前端)、/wrong/add、/wrong/list。后端保持“纯数据”风格一致；不引入云端题库 |

## 七、开发优先级对齐（文档 9 步 → 我方建议路线）
1. P0（微调即做）：结算页本局错题列表(可查释义)；对局顶部暂停+连击倍率展示。
2. P1：词义消消乐（第二玩法；难度=学段/分类；结算/错题/连击规则走全局）。
3. P2（拍板后）：首页“玩法大厅化”；金币/积分（仅站内）+ 结算发放。
4. P3（远期）：错题“用其他玩法练”；连连看/弹弹球/贪吃蛇/24 点；对象存储素材。

## 八、需用户拍板的关键差异
1. **金币/积分**：是否引入站内金币/积分？（建议：先不做真实金币，用现有“星/段位”作长期资产；金币改为纯“单局表现分展示”即可）
2. **连击倍率计分**：保持现状（连击仅视觉）还是做“连击奖分”（需限定不污染服务端 score 口径）？
3. **消消乐难度映射**：直接映射 学段+题型分类（推荐）还是另做 初/中/高 三档？
4. **暂停按钮**：对局中暂停=停止计时/回首页？极速模式暂停策略？
5. **错题复习入口**：每错题“用消消乐练一题”？

> 结论落地前，以上 5 点逐项确认后即可进入 P0 执行。

---

## 九、对齐拍板结论（2026-09-08 评审确认）

1. **资产体系**：以「星 / 段位」为长期资产；**金币不做**（教育合规、避免站内经济复杂度）。
2. **连击倍率**：仅做**视觉展示**（如 ×N 徽标/飘字）；**星星/得分资产不动**，score 口径仍 = 答对数 × 10（服务端推导不变）。
3. **词义消消乐难度**：映射 **学段 + 题型分类**（不另建 初/中/高 三档）。
4. **对局暂停按钮**：对局提供暂停；**暂停时遮罩盖住/隐藏关卡画面**（防止偷看计时与题目）；极速模式暂停时倒计时须停表；提供「继续 / 返回」。
5. **新玩法**（含消消乐、连连看等）→ **统一放后面再规划**；当前批次不新增玩法。

## 十、优化项（对齐后纳入需求）

### O1 闯关界面按分辨率自适应（消除“需下拉滚动”）
- 目标：矮屏手机游戏页一屏放下，不需要滚动。
- 现状：Canvas 固定高（900rpx）+ HUD/模式行/底部面板在部分机型超高，出现下拉滚动。
- 方案：
  - 游戏页容器高度 = 视口（不滚动，恢复 `disableScroll`）；
  - Canvas 不再固定高，占「视口 - HUD - 模式行 - 底部面板」的剩余高度（flex）；
  - 渲染按 `scale = min(实际宽/390, 实际高/500)` **等比缩放并居中**（不变形、不裁切；画布背景与页面同色延伸）；
  - 涉及：`pages/game/game.wxss`（布局）、`pages/game/game.js`（onReady 尺寸计算 + 缩放/居中偏移）。
- 验收：375×667 与更矮屏（如 320×568）试玩均一屏、按钮完整、画面不变形。

### O2 注册/登录流程规范化（参照成熟案例）
- 现状问题：onLaunch 即静默登录建档（“游客”实际已注册）；协议在首页以 modal 摘要弹一次；注册动作自动发生，与预期流程不符。
- 目标流程（成熟做法）：
  1. 进入 = **游客模式**：不建档、纯本地，可玩默认前 3 关；数据本地存储；
  2. 点「微信登录 / 注册」→ 先展示 **用户协议 + 隐私政策**，**同意后**才调用微信接口；
  3. 调 `wx.login` → 后端 `/api/login` **建档（注册成功）** → 返回 token；
  4. `needProfile=true` 引导完善昵称/头像 → 解锁完整功能；
  5. 拒绝协议 → 保持游客；游客本地成绩可提示“登录后同步”（合并可选后续）。
- 改造面：`utils/auth`（登录拆为显式注册入口；不再 onLaunch 自动静默建档）、`app.js`、首页登录入口与协议前置、游客数据本地化与受限规则保持一致。
- 实施方式：先出**详细改造设计**（时序/异常/数据迁移）→ 确认后执行。

### O3（待用户补充）
- 消息中「优化 3、」内容缺失（疑似截断），待用户补充后纳入。

## 十一、执行批次（待用户确认后按批实施）
- 批次 1（UI/布局）：O1 分辨率自适应 → P0：结算页本局错题列表（可查释义）；对局顶部暂停按钮（遮罩隐藏画面，含极速停表）。
- 批次 2：O2 登录/注册流程（先出设计稿确认再改）。
- 批次 3（后续）：玩法扩展（消消乐等）、其他规划。

