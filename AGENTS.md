# 词力战士 · 项目协作约定

> 本文件是给 AI 开发 Agent 的项目级规则，开工前必读。用户明确要求过的约定写在这里，别丢。

## 0. 最高优先级：用户建议/规划不合理时，必须及时提出并等确认

用户 2026-09-12 明确要求：**「我的建议或者规划不合理的时候你要及时找我确认，这个很重要」**。

具体做法：

1. **先给证据，再表态**：引用代码/测试/线上数据的实际结果（文件行号、断言输出、接口返回），说明为什么这条建议按现在的做法会出问题，不要只凭感觉反对。
2. **说清代价**：指出问题分三类并分开讲——
   - 会**做不出来/做不完**（技术不可行、素材成本远超预期）；
   - 做得出来但**玩家体验是坏的**（激励失灵、操作别扭、难度不可达）；
   - 做得出来也还行，但**性价比低**（投入产出不划算、和已有玩法重复）。
3. **给出替代方案**：至少给一个可执行的备选（改小范围、分两期、换实现方式），并说明差异。
4. **涉及方向性决策就先停手等确认**：改玩法规则、改数值口径、改奖励体系、删已有功能、大范围改 UI —— 这类先问，不要自作主张做完再说。
5. **例外**：明显 bug（关卡数学上不可能通关、接口 500、文案错字、测试红）不用等确认，修完连同回归护栏一起汇报。
6. **不许硬着头皮照做**：如果按用户说法实现完会被推翻，先确认比返工便宜 —— 但也不要为了免责把小事都问一遍，
   只在「按你说的做会出问题」时打断。

## 1. 沟通

- 一律**简体中文**回复；关键结论 + 证据（实测数字/截图/断言输出）+ 待确认项。
- 不需要过度格式化，能用短段落说清就别堆列表。
- 如实说明**未验证**的部分，别把「没跑」说成「跑过了」。
- 用户偏好：**先把需求做完再统一验证**，**只要保证提交代码前验证通过**即可；不要每个小改动跑一次全量。

## 2. 改代码

- 一律用 `apply_patch` 改文件。**PowerShell 写文件会毁掉中文**（PS5.1 默认 GBK，已踩坑两次）。
- 控制台里中文显示乱码是 PowerShell 的问题，文件本身是好的 —— 判断编码要看 `git diff`。
- 注释用中文；新增玩法遵循既有结构：引擎模块 `miniprogram/game/xxx.js`（纯逻辑可单测）+ 页面四件套 + 注册 `app.json` 与玩法 tab + 单测。

## 3. 验证（提交前必须全绿）

```bash
cd study_game
node e2e/run-all.js --no-e2e     # 静态 + 单元，约 9 秒：改数据/后端/文档时跑
node e2e/run-all.js              # 全量 11 阶段，约 5 分钟：改前端页面/交互时必须跑
node e2e/run-all.js --only=<id>  # 单点排障：syntax/structure/wxss/unit/game/result/math24/link/snake/pages/m2m4
node e2e/smoke-api.js            # 线上接口冒烟（部署后跑）
```

- 写 E2E 用例的约定见 `e2e/README.md`：**等状态不要等时间**；`_testStep(frames)` 确定性步进；
  连连看复用产品自身的 `canConnect`；贪吃蛇用 `_stopLoop` + `_tick` 冻结主循环后手动步进。
- 改玩法交互时，**测试要跟着交互一起改**（例：删掉滑动转向后，E2E 里所有手势注入都要改成点击）。
- 发现「测试假绿灯」「关卡数学上不可能通过」这类问题**主动挖根因并修**，同时补回归护栏。

## 4. 提交与部署

- 提交信息用中文，分点说明**做了什么 / 为什么 / 怎么验证**；分点用多个 `-m`，**不要在提交信息里用转义引号**（会截断命令）。
- 提交后 `git push origin main`（PowerShell 把 stderr 当错误输出是显示问题，看最后一行是否出现 `<old>..<new>  main -> main`）。
- 云托管**Git 自动部署链路是坏的**（报 `blob unknown to registry`，平台侧问题），用 CLI：

```bash
cloudbase cloudrun deploy -e prod-d6gnifjoe28cfd96f -s express-g0hk --source . --port 80
# 「About to start deployment」→ y；「Enable gray deployment」→ 直接回车（No）
cloudbase cloudrun list -e prod-d6gnifjoe28cfd96f   # 看服务状态与更新时间
```

- 只有**后端/容器**改动才需要云托管部署；纯小程序前端或纯文档改动不必部署，
  前端要在微信开发者工具里预览/上传才生效。
- 改 `server/models/` 必须把「生产库 DDL」写进发布清单（历史上漏过 `scores.game_type/type_key`）。

## 5. 环境注意

- 微信开发者工具：`D:\Program Files (x86)\Tencent\微信web开发者工具`；模拟器截图在窗口不在前台时 rAF 被节流 → 画布空白，属正常现象不是 bug。
- 子代理：本环境 `spawn_agent/followup_task` 的任务正文有时送不到子代理，默认**串行推进**；用户明确要求并行时再试。

## 6. 资源与包体上限（硬性，用户 2026-09-12 明确要求）

**图片和音频，单个文件都不能超过 200KB。** 这是平台侧的单文件硬限，和「整包 ≤2MB」是两件事，必须分别满足。

- 新增任何图片/音频（美术皮肤、成就图标、段位徽章、音效、关卡素材）前先看体积；
  超标就往 `miniprogram/assets-src/` 放原图（**该目录不参与打包**），端上只放压缩后的展示尺寸。
- 压缩走现成脚本：`python e2e/compress-assets.py`。
- 项目自留的更严目标：**图片压到 60KB 以内**（只提醒不阻断，但超了整包很快会顶到 1.8MB 告警线）。
- 校验：`node e2e/check-assets.js`（已接入全量验证的静态层，超标直接失败、退出码 1）；
  `miniprogram/utils/__tests__/assets-limit.test.js` 守着这两个阈值不被改宽。
- 音频现状：包内**没有任何音频文件** —— 音效是 `game/audio.js` 用 WebAudio 现场合成的，发音走 TTS 插件。
  将来要加音频资源，同样受 200KB 限制。
