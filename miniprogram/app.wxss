/**
 * app.wxss —— 词力战士全局样式
 *
 * 设计说明：
 *   - 配色取自 HTML 试玩原型（prototype/index.html）的暖色天空卡通风格
 *   - 卡通扁平化：大圆角 + 底部硬阴影（box-shadow 0 Xpx 0）营造「按压感」
 *   - 提供通用按钮 / 卡片 / 背景基线样式，各页面可复用或覆盖
 */

/* ============ 全局基座 ============ */
page {
  /* 卡通配色变量（供各页面引用） */
  --sky1: #7ec8ff;      /* 天空浅蓝 */
  --sky2: #d8efff;      /* 天空更浅蓝 */
  --pink: #ff5d8f;      /* 卡通粉 */
  --orange: #ffb703;    /* 卡通橙 */
  --blue: #4cc9f0;      /* 卡通蓝 */
  --green: #7bd389;     /* 卡通绿 */
  --purple: #b388ff;    /* 卡通紫 */
  --ink: #3a3a5c;       /* 主文字墨色 */
  --text-sub: #5c7f9e;  /* 次要文字 */
  --gold: #ffd166;      /* 星星/成就金 */
  --danger: #ff5a5a;    /* 危险/错误红 */

  /* 暖色天空渐变背景 */
  background: linear-gradient(180deg, #5aa9e6 0%, #a3d5ff 60%, #d8efff 100%);
  background-attachment: fixed;
  font-family: "PingFang SC", "Microsoft YaHei", system-ui, sans-serif;
  color: #3a3a5c;
  font-size: 28rpx;
  min-height: 100vh;
  box-sizing: border-box;
}

/* 页面容器（全屏纵向布局） */
.page {
  display: flex;
  flex-direction: column;
  align-items: center;
  min-height: 100vh;
  padding: 0 40rpx;
  box-sizing: border-box;
}

/* ============ 标题 ============ */
.page-title {
  font-size: 48rpx;
  font-weight: 900;
  color: #ffffff;
  letter-spacing: 8rpx;
  text-shadow: 0 6rpx 0 rgba(0, 0, 0, 0.12);
  margin: 40rpx 0 0;
}

.page-sub {
  color: #5c7f9e;
  font-size: 26rpx;
  letter-spacing: 8rpx;
  margin: 12rpx 0 40rpx;
}

/* ============ 通用按钮（卡通硬阴影风格） ============ */
/**
 * ============ 通用「游戏页外壳」（2026-09-12 需求⑥ 视觉统一） ============
 *
 * 老玩法页（数独/2048/24 点/连连看/贪吃蛇/消消乐）此前各写各的头部与 HUD，
 * 字号、圆角、留白、胶囊配色都不一致，看起来像不同游戏。这里把「头部 + HUD」抽成一套共享类：
 *   .g-head / .g-title / .g-chip      顶部栏（返回胶囊 + 标题 + 右侧信息胶囊）
 *   .g-hud / .g-hud-box / .g-hud-num / .g-hud-lab   两到四个指标盒
 * 各页只需在 wxml 上套这些类，删掉各自重复的样式即可保持一致。
 */
.g-head {
  display: flex;
  align-items: center;
  gap: 16rpx;
  flex: 0 0 auto;
}

.g-title {
  font-size: 38rpx;
  font-weight: 900;
  color: #fff;
  text-shadow: 0 4rpx 0 rgba(0, 0, 0, .12);
}

.g-chip {
  margin-left: auto;
  background: rgba(255, 255, 255, .92);
  color: #5c7f9e;
  font-size: 24rpx;
  font-weight: 800;
  padding: 10rpx 22rpx;
  border-radius: 999rpx;
}

.g-hud {
  display: flex;
  gap: 14rpx;
  margin: 18rpx 0 12rpx;
  flex: 0 0 auto;
}

.g-hud-box {
  flex: 1;
  background: rgba(255, 255, 255, .94);
  border-radius: 20rpx;
  padding: 12rpx 8rpx;
  text-align: center;
}

.g-hud-num { font-size: 32rpx; font-weight: 900; color: #3a3a5c; }
.g-hud-lab { font-size: 20rpx; color: #8a9bb5; margin-top: 4rpx; }

/* 胶囊型 HUD（数独/24 点/连连看/贪吃蛇/消消乐共用；原来 5 个页面各写一份，只有 padding 和字号差 1~2rpx） */
.hud {
  display: flex;
  justify-content: center;
  gap: 18rpx;
  margin: 18rpx 0 14rpx;
  flex: 0 0 auto;
}

.hud-chip {
  background: rgba(255, 255, 255, .92);
  color: #3a3a5c;
  border-radius: 999rpx;
  padding: 12rpx 30rpx;
  font-size: 26rpx;
  font-weight: 800;
  box-shadow: 0 6rpx 0 rgba(0, 0, 0, .05);
}

.btn {
  border: none;
  border-radius: 44rpx;
  font-weight: 900;
  color: #ffffff;
  text-align: center;
  letter-spacing: 4rpx;
  box-shadow: 0 12rpx 0 rgba(0, 0, 0, 0.18), 0 20rpx 40rpx rgba(0, 0, 0, 0.12);
  transition: transform 0.12s;
}

/**
 * 重置小程序原生 <button> —— 关键修复（2026-09-12 用户反馈「很多按钮里的字靠上」）：
 *
 * 原生 button 自带 `line-height: 2.55555556`（≈46px）；而这里原来写的是 `line-height: inherit`，
 * 父容器没设行高时它会退化成 `normal`。两种情况都会让「设了 height 但没写 line-height」的按钮
 * 里文字贴顶（各页的 .btn-back / .btn-use / .check-pill 等都是这一类）。
 *
 * 统一改成 flex 垂直居中：不论按钮多高、字号多大，文字都在正中。
 * 页面里已经写了 `line-height: <height>` 的按钮不受影响（两者效果一致）。
 */
button {
  display: flex;
  align-items: center;
  justify-content: center;
  line-height: 1.2;
}
button::after {
  border: none;
}

/* 自定义 .btn 风格（去掉原生内边距/外边距，颜色由 .btn-xxx 决定） */
button.btn {
  padding: 0;
  margin: 0;
  background: #ffb703;
}
.btn:active {
  transform: translateY(6rpx);
  box-shadow: 0 6rpx 0 rgba(0, 0, 0, 0.18);
}

/* 主按钮（大号，常用于「开始游戏」） */
.btn-primary {
  width: 480rpx;
  height: 128rpx;
  font-size: 44rpx;
  background: #ffb703;
  line-height: 128rpx;
}

/* 次级按钮（颜色可分主题覆写） */
.btn-secondary {
  min-width: 240rpx;
  padding: 0 40rpx;
  height: 88rpx;
  font-size: 32rpx;
  background: #4cc9f0;
  line-height: 88rpx;
}

/* 按钮配色变体（对应原型 .btn-xxx） */
.btn-blue { background: #4cc9f0; }
.btn-purple { background: #b388ff; }
.btn-green { background: #7bd389; }
.btn-pink { background: #ff5d8f; }
.btn-orange { background: #ffb703; }

/* ============ 通用卡片 ============ */
.card {
  background: #ffffff;
  border-radius: 40rpx;
  box-shadow: 0 12rpx 0 rgba(0, 0, 0, 0.1), 0 24rpx 48rpx rgba(0, 0, 0, 0.12);
  padding: 40rpx;
}

/* ============ 布局辅助 ============ */
/* 页面内按钮/元素之间的垂直间距 */
.menu-gap { margin-top: 32rpx; }

/* 占位内容卡片（骨架期提示后续任务实现） */
.placeholder {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 240rpx;
  color: #8899aa;
  font-size: 30rpx;
  text-align: center;
}

/* ============ 返回按钮 ============ */
.back-btn {
  align-self: flex-start;
  margin: 24rpx 0;
  width: 88rpx;
  height: 88rpx;
  border-radius: 28rpx;
  background: rgba(255, 255, 255, 0.85);
  color: #3a3a5c;
  font-size: 40rpx;
  font-weight: 900;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 6rpx 16rpx rgba(0, 0, 0, 0.12);
}

/* ============ 辅助组件（UI 重设计新增，各页复用） ============ */

/* 软卡片变体：浅底、更轻阴影（用于内嵌信息卡） */
.card--soft {
  background: rgba(255, 255, 255, 0.85);
  box-shadow: 0 6rpx 0 rgba(0, 0, 0, 0.06), 0 12rpx 24rpx rgba(0, 0, 0, 0.06);
}

/* 标签 / 徽章（稀有度、成就、签到天数等） */
.tag,
.badge {
  display: inline-block;
  padding: 4rpx 16rpx;
  border-radius: 20rpx;
  font-size: 22rpx;
  font-weight: 700;
  line-height: 1.4;
}
.tag {
  background: rgba(255, 255, 255, 0.85);
  color: #5c7f9e;
}
.badge {
  background: #ffd166;
  color: #6b4e00;
}
.badge--green { background: #7bd389; color: #fff; }
.badge--blue { background: #4cc9f0; color: #fff; }
.badge--pink { background: #ff5d8f; color: #fff; }
.badge--purple { background: #b388ff; color: #fff; }

/* 大 emoji 头像容器（皮肤/吉祥物展示） */
.emoji-avatar {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 160rpx;
  height: 160rpx;
  border-radius: 50%;
  background: #ffffff;
  box-shadow: 0 8rpx 0 rgba(0, 0, 0, 0.08), 0 16rpx 32rpx rgba(0, 0, 0, 0.08);
}
.emoji-avatar text {
  font-size: 96rpx;
  line-height: 1;
}

/* 空态占位（统一风格） */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 240rpx;
  color: #8899aa;
  font-size: 28rpx;
  text-align: center;
}
.empty-state .empty-emoji {
  font-size: 88rpx;
  line-height: 1;
  margin-bottom: 16rpx;
}

/* 进度条（错题本熟练度、成就进度等） */
.progress-bar {
  width: 100%;
  height: 20rpx;
  border-radius: 10rpx;
  background: rgba(0, 0, 0, 0.08);
  overflow: hidden;
}
.progress-bar__fill {
  height: 100%;
  border-radius: 10rpx;
  background: linear-gradient(90deg, #4cc9f0, #7bd389);
  transition: width 0.3s ease;
}

/* ============ 棋盘 / 卡牌材质（需求⑥ 第二期，6 个棋盘类玩法共用） ============
   一期把头部与 HUD 统一了，二期统一「棋盘本身」的质感：原来各页的格子都是一块纯色方块
   加一层几乎看不见的阴影，整块棋盘像浮在页面背景上。这里定四个共用类：

     .b-tray  棋盘底槽 —— 让棋子「嵌」在板上
     .b-face  棋子/卡牌面 —— 白面渐变 + 底部硬阴影 + 顶部内高光（卡通塑料片质感）
     .b-press 按下反馈 —— 下沉并收起投影，与 app.wxss 的 .btn:active 同一套手感
     .b-sel   选中态 —— 金色环 + 上浮（用 box-shadow 画环，不依赖 border，
                        因为各页原来有的写了 outline、有的只写了 border-color 而没声明 border）

   注意：数字类玩法（2048 的 .cell.c8 等）用「两个类」的选择器设背景，特异性高于单类名
   的 .b-face，所以叠上去不会把颜色冲掉 —— 这正是把材质做成独立类而不是写进 .cell 的原因。 */
.b-tray {
  background: rgba(255, 255, 255, 0.17);
  border: 2rpx solid rgba(255, 255, 255, 0.3);
  border-radius: 26rpx;
  padding: 16rpx;
  box-sizing: border-box;
  box-shadow: inset 0 8rpx 18rpx rgba(16, 44, 78, 0.16);
}
.b-face {
  background: linear-gradient(180deg, #ffffff 0%, #eef4fb 100%);
  box-shadow: 0 6rpx 0 rgba(18, 44, 78, 0.16), 0 8rpx 16rpx rgba(18, 44, 78, 0.12),
    inset 0 2rpx 0 rgba(255, 255, 255, 0.95);
}
.b-press:active {
  transform: translateY(3rpx);
  box-shadow: 0 3rpx 0 rgba(18, 44, 78, 0.16), 0 4rpx 8rpx rgba(18, 44, 78, 0.1),
    inset 0 2rpx 0 rgba(255, 255, 255, 0.95);
}
.b-sel {
  transform: translateY(-4rpx);
  box-shadow: 0 0 0 6rpx #ffb703, 0 6rpx 0 rgba(180, 120, 0, 0.3),
    0 8rpx 16rpx rgba(180, 120, 0, 0.2), inset 0 2rpx 0 rgba(255, 255, 255, 0.95);
}
/* 入场：整块棋盘轻微铺开（一次性，不打扰对局节奏） */
@keyframes bBoardIn {
  from { opacity: 0; transform: translateY(-10rpx) scale(0.985); }
  to { opacity: 1; transform: none; }
}
.b-in { animation: bBoardIn 0.26s ease-out both; }

/* ============ 深色模式兜底：已移除（2026-09-12） ============
   背景：这里原先有一段 `@media (prefers-color-scheme: dark)`，只把 page 底色改成近黑 (#12171f)、
   文字改成浅色，但**页面内容（卡片、渐变、按钮、图例）全是按浅色主题设计的，没有任何一套深色适配**。
   后果（真机实测）：18 个「自己没有背景色、依赖 page 底」的页面在深色模式手机上**整片发黑、内容看不清**
   —— 我的 / 成就 / 关卡 / 拼词 / 结算 / 错题本等，而开发者工具默认浅色，本地完全复现不出来。
   结论：对这款卡通教育游戏，**统一浅色主题**才是正确做法（多数页面本身就写死了浅色渐变背景），
   故移除该兜底。将来真要支持深色模式，必须逐页做完整的深色配色（不能只改 page 底），
   并补一条「深色模式截图回归」的用例，否则会重现同样的问题。 */
